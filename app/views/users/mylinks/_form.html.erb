<!-- <%= form_with(model: mylink, local: true) do |form| %> -->
<%= form_for [@user, @mylink] do |form| %>
  <% if mylink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mylink.errors.count, "error") %> prohibited this mylink from being saved:</h2>

      <ul>
      <% mylink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :mylink_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  
<% end %>

<% end %>